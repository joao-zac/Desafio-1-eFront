const Form = () => {
  return (
    <form action="">
        <input type="text" />
        <label htmlFor="Nome">Nome</label>

        <input type="text" />
        <label htmlFor="Nome">Email</label>

        <button>Enviar</button>
    </form>
  )
}

export default Form